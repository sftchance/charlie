import { ENS } from "@ensdomains/ensjs";

import { User } from "../types";

import { providers } from "../utils/config";

const useENSMulticall = (addresses: string[]) => {
    // const ENSInstance = new ENS()
    // let users: User[] = [];

    // for (const address of addresses) {
    //     const calls: {
    //         target: `0x${string}`,
    //         allowFailure: boolean,
    //         callData: string,
    //     }[] = []

    //     calls.push({

    // }

    // return {
    //     users
    // }
}

export { useENSMulticall }